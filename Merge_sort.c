#include <stdio.h>
#include <stdlib.h>

//bigO(nLog_n)

void merge(int* left,int* right, int* all)
{
    
}

int main()
{
    int unsortedArray[] = {2,4,1,6,8,5,3,7};
    int mid = sizeof(unsortedArray)/sizeof(int);

    return 0;
}